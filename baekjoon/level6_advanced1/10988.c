#include <stdio.h>
#include <string.h>
#define MAX_LENGTH 100

int main() {
    char str[MAX_LENGTH];
    scanf("%s", str);
    printf("%s", str);

    for (int i = 0; i <= strlen(str) / 2; i++) {
        
    }
}